[package]
name = "domain"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "domain"
path = "src/lib.rs"

[features]
postgres = []
mysql = []

[dependencies]
types = { path = "../types", features = ["postgres"] }
repository = { path = "../repository", features = ["postgres"] }
redis = { path = "../redis" }
search = { path = "../search", features = ["postgres"] }
serde = { version = "1.0.125", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4.19", features = ["serde"] }
itertools = "0.9.0"
thiserror = "1.0.20"
bcrypt = "0.8.1"
anyhow = "1.0.40"
async-trait = "0.1"
flexi_logger = "0.19.3"
log = "0.4"
meilisearch-sdk = { version = "0.10", default-features = false, features = [
    "sync",
] }
