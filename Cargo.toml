[workspace]
members = [
    "warp-api",
    "repository",
    "domain",
    "types",
    "redis",
    "search",
    "axum-api",
    "server",
    "auth",
]

[profile.dev]
codegen-units = 16

[profile.release]
lto = true
opt-level = 'z'
